/**
 * Identidade do produto.
 *
 * Saiu de `src/mocks/` quando os dados passaram a vir do Supabase: o nome do
 * sistema nunca foi dado de demonstração, e deixá-lo lá faria a pasta de mocks
 * sobreviver só por causa dele.
 *
 * Continua sendo o único lugar onde o nome aparece — trocar o nome do sistema
 * é alterar este objeto.
 */
export const brand = {
  name: "Vita",
  tagline: "Gestão financeira",
};
